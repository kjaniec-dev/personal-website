Before considering work complete in this repo, run the relevant verification commands. Minimum default checks for UI/code changes: `npm run lint` and `npm run test`. For build-sensitive or routing/layout work, also run `npm run build`. If content or SEO behavior changed, sanity-check relevant routes locally with `npm run dev`. Do not claim success without reporting which commands were run and whether any checks were skipped.