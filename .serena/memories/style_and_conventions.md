Code style is TypeScript/JSX with Tailwind utility classes and App Router conventions. Imports often use `@/...` aliases. Formatting is enforced by Biome with tabs for indentation and double quotes in JS/TS. Tailwind classes are written inline in components. The codebase uses React function components, mostly default exports for route components, and utility/data files in `data/`. TypeScript has `strictNullChecks: true` but `strict: false`, so type discipline is mixed. Keep edits aligned with existing Tailwind-heavy, component-based patterns rather than introducing a separate styling system.